describe("User Module", () => {

    it("is just a test", () => {
        const awaited = 5;
        const response = (2 + 3);

        expect(response).toBe(awaited);
    });

});

describe("Accomodation Module", () => {

    it("must return an accomodation", () => {
        const awaited = "accomodation";
        const response = "accomodation";

        expect(response).toBe(awaited);
    })

})